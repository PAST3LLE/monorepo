import { Row } from '@past3lle/components'
import styled from 'styled-components'

export const StyledSkillpoint = styled(Row)<{ active: boolean }>`
  position: relative;
  cursor: pointer;
  width: 10vh;
  height: 10vh;
  padding: 3px;
`
